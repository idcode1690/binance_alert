# Root Wrangler config for Cloudflare Pages
name = "binance-alert"
compatibility_date = "2025-08-16"

# Required for Cloudflare Pages build system (v2 root strategy)
pages_build_output_dir = "build"

# Optional future flags (uncomment if needed)
# compatibility_flags = ["nodejs_compat"]

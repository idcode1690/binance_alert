{
  "name": "binance_alert",
  "version": "0.1.0",
  "private": true,
  "homepage": ".",
  "dependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^1.13.1",
    "prop-types": "^15.8.1",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "^7.9.5",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "test:telegram": "node scripts/test_telegram.js",
    "health": "node -e \"(async()=>{const u=(process.env.WORKER_URL||'https://binance-alert.idcode1690.workers.dev').replace(/\\\\\\/$/,'');const r=await fetch(u+'/health');console.log(await r.text())})()\"",
    "pages:dev": "wrangler pages dev ./build",
    "pages:deploy": "wrangler pages deploy ./build",
    "worker:deploy": "wrangler deploy -c wrangler.worker.toml",
    "worker:dev": "wrangler dev -c wrangler.worker.toml",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "yaml": "^2.8.1"
  }
}

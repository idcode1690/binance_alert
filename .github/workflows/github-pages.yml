name: "DISABLED: GitHub Pages (use gh-pages.yml)"

on:
  # 수동 실행만 허용하고, 내용은 no-op으로 유지
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is disabled. Use gh-pages.yml for GitHub Pages."

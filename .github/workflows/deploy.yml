name: (Disabled) Deploy Cloudflare Pages

on:
  # 완전 비활성화: 수동 실행만 허용하고, 잡은 no-op 처리
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled notice
        run: |
          echo "This workflow is disabled. We use GitHub Pages for frontend and Cloudflare Worker for backend."


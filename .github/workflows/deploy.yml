name: "DEPRECATED: Do not use (use cloudflare-pages.yml)"

on:
  # 완전 비활성화: 수동 실행만 허용하고, 잡은 no-op 처리
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled notice
        run: |
          echo "This workflow is deprecated. Use .github/workflows/cloudflare-pages.yml for Cloudflare Pages, or github-pages.yml for GitHub Pages."

